% Aluno: Esdras Battosti da Silva Ra: 2143470

% Faca um algoritmo para encontrar e apresentar o valor para Z aplicando a seguinte formula:
% Z=(L+A * 2) / C. Os valores L, A e C sao dados de entrada.

L = input('type the L value: ');
A = input('type the A value: ');
C = input('type the C value: ');

fprintf('Z = (L + A*2)/ C = %.2f\n', (L+ A*2)/C)